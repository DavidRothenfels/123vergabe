{
  "name": "opencode-multiuser",
  "version": "1.0.0",
  "description": "Multiuser OpenCode system with PocketBase backend",
  "main": "opencode-service.js",
  "scripts": {
    "start": "node opencode-service.js",
    "dev": "nodemon opencode-service.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t opencode-multiuser .",
    "docker:run": "docker run -p 8090:8090 -p 3001:3001 -e OPENAI_API_KEY=\"$OPENAI_API_KEY\" opencode-multiuser"
  },
  "keywords": [
    "opencode",
    "multiuser",
    "pocketbase",
    "ai",
    "code-generation"
  ],
  "author": "OpenCode Team",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}